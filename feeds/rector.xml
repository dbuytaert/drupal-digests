<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>
    <title>Drupal Rector Rules</title>
    <description>Runnable Rector rules extracted from Drupal issue discussions. Drop them into your project to automatically rewrite deprecated code.</description>
    <link>https://github.com/dbuytaert/drupal-digests</link>
    <item>
      <title>Inline user_picture_enabled() calls into a field-definition check</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/inline-user-picture-enabled-calls-into-a-field-definition-3151555.php</link>
      <guid isPermaLink="false">01a0597b-f570-76ed-aa4e-ad92bcc4d3a3</guid>
      <pubDate>Mon, 31 Aug 2026 20:21:31 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3151555"&gt;#3151555&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;user_picture_enabled()&lt;/code&gt; is deprecated with no direct replacement; callers must inline the check it used to perform. This rule rewrites zero-argument calls to &lt;code&gt;user_picture_enabled()&lt;/code&gt; into &lt;code&gt;isset(\Drupal::service('entity_field.manager')-&amp;gt;getFieldDefinitions('user', 'user')['user_picture'])&lt;/code&gt;, matching exactly what core's own call sites were changed to. It preserves surrounding boolean context (negation, &lt;code&gt;&amp;amp;&amp;amp;&lt;/code&gt;, etc.) since the replacement is a drop-in boolean expression.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;if (!user_picture_enabled()) {
  $disabled['toggle_node_user_picture'] = TRUE;
}

if (!empty($build['user_picture']) &amp;amp;&amp;amp; user_picture_enabled()) {
  // ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;if (!isset(\Drupal::service('entity_field.manager')-&amp;gt;getFieldDefinitions('user', 'user')['user_picture'])) {
  $disabled['toggle_node_user_picture'] = TRUE;
}

if (!empty($build['user_picture']) &amp;amp;&amp;amp; isset(\Drupal::service('entity_field.manager')-&amp;gt;getFieldDefinitions('user', 'user')['user_picture'])) {
  // ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only matches calls with zero arguments (the function's real signature). A user-defined function or method that happens to share the name &lt;code&gt;user_picture_enabled&lt;/code&gt; but takes arguments, or is called as &lt;code&gt;$this-&amp;gt;user_picture_enabled(...)&lt;/code&gt;, is left untouched since the rule only targets global &lt;code&gt;FuncCall&lt;/code&gt; nodes.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ArrayDimFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\Isset_;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceUserPictureEnabledRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated user_picture_enabled() with an inline check of the user_picture field definition.',
            [new CodeSample(
                'if (!user_picture_enabled()) { }',
                &amp;quot;if (!isset(\\Drupal::service('entity_field.manager')-&amp;gt;getFieldDefinitions('user', 'user')['user_picture'])) { }&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        if ($node-&amp;gt;isFirstClassCallable()) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'user_picture_enabled')) {
            return null;
        }
        if (count($node-&amp;gt;args) !== 0) {
            return null;
        }

        $service = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new String_('entity_field.manager'))],
        );
        $getFieldDefinitions = new MethodCall(
            $service,
            'getFieldDefinitions',
            [new Arg(new String_('user')), new Arg(new String_('user'))],
        );
        $arrayDimFetch = new ArrayDimFetch($getFieldDefinitions, new String_('user_picture'));

        return new Isset_([$arrayDimFetch]);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceUserPictureEnabledRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace deprecated update.module global functions with service calls</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-deprecated-update-module-global-functions-with-3580703.php</link>
      <guid isPermaLink="false">01a057d1-2431-70bb-a3e7-73ce8b714f8b</guid>
      <pubDate>Mon, 31 Aug 2026 12:35:19 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3580703"&gt;#3580703&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 deprecates five &lt;code&gt;update.module&lt;/code&gt; procedural functions in favor of methods on the &lt;code&gt;update.manager&lt;/code&gt; and &lt;code&gt;update.processor&lt;/code&gt; services, removed in Drupal 13. This rule rewrites &lt;code&gt;update_get_available()&lt;/code&gt;, &lt;code&gt;update_refresh()&lt;/code&gt;, &lt;code&gt;update_storage_clear()&lt;/code&gt;, &lt;code&gt;update_create_fetch_task()&lt;/code&gt;, and &lt;code&gt;update_fetch_data()&lt;/code&gt; calls to the equivalent &lt;code&gt;\Drupal::service(...)-&amp;gt;method(...)&lt;/code&gt; call, preserving all arguments. It skips dynamic calls (&lt;code&gt;$fn()&lt;/code&gt;) where the function name is not statically known, and leaves unrelated functions of the same short name alone.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$available = update_get_available(TRUE);
update_refresh();
update_storage_clear();
update_create_fetch_task($project);
update_fetch_data();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$available = \Drupal::service('update.manager')-&amp;gt;getAvailable(TRUE);
\Drupal::service('update.manager')-&amp;gt;refreshUpdateData();
\Drupal::service('update.manager')-&amp;gt;reset();
\Drupal::service('update.processor')-&amp;gt;createFetchTask($project);
\Drupal::service('update.processor')-&amp;gt;fetchData();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Does not cover &lt;code&gt;update_fetch_data_finished()&lt;/code&gt; or &lt;code&gt;_update_project_status_sort()&lt;/code&gt;, which core deprecates with no replacement, nor &lt;code&gt;_update_no_data()&lt;/code&gt;/&lt;code&gt;_update_message_text()&lt;/code&gt;, whose replacements are protected methods on &lt;code&gt;\Drupal\update\UpdateMessageTrait&lt;/code&gt; that cannot be called without a class using that trait; those call sites need manual review.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceUpdateModuleFunctionsRector extends AbstractRector
{
    /**
     * Maps a deprecated update.module global function to the [service id,
     * method name] that replaces it. All of these functions were deprecated
     * in drupal:11.5.0 and are removed from drupal:13.0.0.
     *
     * @var array&amp;lt;string, array{0: string, 1: string}&amp;gt;
     */
    private const FUNCTION_MAP = [
        'update_get_available' =&amp;gt; ['update.manager', 'getAvailable'],
        'update_refresh' =&amp;gt; ['update.manager', 'refreshUpdateData'],
        'update_storage_clear' =&amp;gt; ['update.manager', 'reset'],
        'update_create_fetch_task' =&amp;gt; ['update.processor', 'createFetchTask'],
        'update_fetch_data' =&amp;gt; ['update.processor', 'fetchData'],
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated update.module global functions with calls to the update.manager and update.processor services.',
            [new CodeSample(
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
$available = update_get_available(TRUE);
update_storage_clear();
CODE_SAMPLE,
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
$available = \Drupal::service('update.manager')-&amp;gt;getAvailable(TRUE);
\Drupal::service('update.manager')-&amp;gt;reset();
CODE_SAMPLE,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        if (!$node-&amp;gt;name instanceof Name) {
            // Dynamic call, e.g. $fn(); the function name isn't statically known.
            return null;
        }
        $functionName = $this-&amp;gt;getName($node-&amp;gt;name);
        if ($functionName === null || !isset(self::FUNCTION_MAP[$functionName])) {
            return null;
        }
        [$serviceId, $method] = self::FUNCTION_MAP[$functionName];

        $serviceCall = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new String_($serviceId))],
        );

        return new MethodCall($serviceCall, $method, $node-&amp;gt;args);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceUpdateModuleFunctionsRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace user_login_finalize() and user_logout() with services</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-user-login-finalize-and-user-logout-with-services-2012976.php</link>
      <guid isPermaLink="false">01a04a68-fab4-7012-aef0-55439f7656d5</guid>
      <pubDate>Fri, 28 Aug 2026 22:06:29 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/2012976"&gt;#2012976&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 deprecates the procedural &lt;code&gt;user_login_finalize()&lt;/code&gt; and &lt;code&gt;user_logout()&lt;/code&gt; functions in &lt;code&gt;user.module&lt;/code&gt; in favor of &lt;code&gt;\Drupal\user\LoginFinalizer::finalizeLogin()&lt;/code&gt; and &lt;code&gt;\Drupal\user\LogoutFinalizer::finalizeLogout()&lt;/code&gt;, obtained via dependency injection or &lt;code&gt;\Drupal::service()&lt;/code&gt;. This rule rewrites call sites of both global functions to the equivalent service call, letting contrib and custom code (including auth/session modules that call these directly) migrate ahead of removal in drupal:13.0.0.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;user_login_finalize($account);
user_logout();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;\Drupal::service(\Drupal\user\LoginFinalizer::class)-&amp;gt;finalizeLogin($account);
\Drupal::service(\Drupal\user\LogoutFinalizer::class)-&amp;gt;finalizeLogout();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Skips call sites that use named arguments (e.g. &lt;code&gt;user_login_finalize(account: $account)&lt;/code&gt;) because the new service method's parameter is named &lt;code&gt;$user&lt;/code&gt;, not &lt;code&gt;$account&lt;/code&gt;; rewriting those would break under strict named-argument binding. Such (rare) call sites are left untouched for manual review. The rule targets &lt;code&gt;\Drupal::service()&lt;/code&gt; call sites; it does not inject the service into a class constructor, since that requires editing the class's dependency list which is outside the scope of this rewrite.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceUserLoginLogoutFinalizeRector extends AbstractRector
{
    /**
     * Maps deprecated global function name to [service FQCN, method name].
     *
     * @var array&amp;lt;string, array{0: string, 1: string}&amp;gt;
     */
    private const REPLACEMENTS = [
        'user_login_finalize' =&amp;gt; ['Drupal\\user\\LoginFinalizer', 'finalizeLogin'],
        'user_logout' =&amp;gt; ['Drupal\\user\\LogoutFinalizer', 'finalizeLogout'],
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated user_login_finalize() and user_logout() global functions with the LoginFinalizer and LogoutFinalizer services.',
            [new CodeSample(
                'user_login_finalize($account);
user_logout();',
                '\Drupal::service(\Drupal\user\LoginFinalizer::class)-&amp;gt;finalizeLogin($account);
\Drupal::service(\Drupal\user\LogoutFinalizer::class)-&amp;gt;finalizeLogout();',
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }

        foreach (self::REPLACEMENTS as $functionName =&amp;gt; [$serviceClass, $methodName]) {
            if (!$this-&amp;gt;isName($node-&amp;gt;name, $functionName)) {
                continue;
            }

            // The new service method's parameter name differs from the old
            // function's parameter name; a named argument would break.
            foreach ($node-&amp;gt;args as $arg) {
                if ($arg instanceof Arg &amp;amp;&amp;amp; $arg-&amp;gt;name !== null) {
                    return null;
                }
            }

            $serviceCall = new StaticCall(
                new FullyQualified('Drupal'),
                'service',
                [new Arg(new ClassConstFetch(new FullyQualified($serviceClass), 'class'))],
            );

            return new MethodCall($serviceCall, $methodName, $node-&amp;gt;args);
        }

        return null;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceUserLoginLogoutFinalizeRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace _user_mail_notify() calls with NotificationHandler methods</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-user-mail-notify-calls-with-notificationhandler-3539178.php</link>
      <guid isPermaLink="false">01a03fe4-4974-7368-98e4-e18f672c7007</guid>
      <pubDate>Wed, 26 Aug 2026 21:05:20 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3539178"&gt;#3539178&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 deprecates the global function &lt;code&gt;_user_mail_notify($op, $account)&lt;/code&gt; in favor of dedicated methods on the internal &lt;code&gt;Drupal\user\NotificationHandler&lt;/code&gt; service, one method per notification type. This rule rewrites call sites where the &lt;code&gt;$op&lt;/code&gt; argument is a literal string it recognizes (e.g. &lt;code&gt;password_reset&lt;/code&gt;, &lt;code&gt;cancel_confirm&lt;/code&gt;, &lt;code&gt;status_blocked&lt;/code&gt;) into the equivalent &lt;code&gt;\Drupal::service(NotificationHandler::class)-&amp;gt;sendXxx($account)&lt;/code&gt; call, letting contrib modules migrate ahead of removal in Drupal 13.0.0.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;_user_mail_notify('password_reset', $account);
_user_mail_notify('cancel_confirm', $entity);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;\Drupal::service(\Drupal\user\NotificationHandler::class)-&amp;gt;sendPasswordReset($account);
\Drupal::service(\Drupal\user\NotificationHandler::class)-&amp;gt;sendCancelConfirm($entity);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites calls whose &lt;code&gt;$op&lt;/code&gt; argument is a plain string literal matching one of the eight known operations (&lt;code&gt;register_admin_created&lt;/code&gt;, &lt;code&gt;register_no_approval_required&lt;/code&gt;, &lt;code&gt;register_pending_approval&lt;/code&gt;, &lt;code&gt;password_reset&lt;/code&gt;, &lt;code&gt;status_activated&lt;/code&gt;, &lt;code&gt;status_blocked&lt;/code&gt;, &lt;code&gt;cancel_confirm&lt;/code&gt;, &lt;code&gt;status_canceled&lt;/code&gt;). Calls that compute &lt;code&gt;$op&lt;/code&gt; dynamically (e.g. &lt;code&gt;$op = $active ? 'status_activated' : 'status_blocked'; _user_mail_notify($op, $account);&lt;/code&gt;), use named arguments, pass a custom/unsupported &lt;code&gt;$op&lt;/code&gt; value, or pass a different argument count are left untouched and must be migrated by hand. The old function could return &lt;code&gt;NULL&lt;/code&gt; (suppressed) or &lt;code&gt;FALSE&lt;/code&gt; (error) while the new methods always return &lt;code&gt;bool&lt;/code&gt;; call sites that rely on distinguishing &lt;code&gt;NULL&lt;/code&gt; from &lt;code&gt;FALSE&lt;/code&gt; (rather than a simple truthy/falsy check) need manual review.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceUserMailNotifyRector extends AbstractRector
{
    private const OP_TO_METHOD = [
        'register_admin_created' =&amp;gt; 'sendRegisterAdminCreated',
        'register_no_approval_required' =&amp;gt; 'sendRegisterNoApprovalRequired',
        'register_pending_approval' =&amp;gt; 'sendRegisterPendingApproval',
        'password_reset' =&amp;gt; 'sendPasswordReset',
        'status_activated' =&amp;gt; 'sendStatusActivated',
        'status_blocked' =&amp;gt; 'sendStatusBlocked',
        'cancel_confirm' =&amp;gt; 'sendCancelConfirm',
        'status_canceled' =&amp;gt; 'sendStatusCanceled',
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace _user_mail_notify() calls with the equivalent Drupal\user\NotificationHandler method.',
            [new CodeSample(
                &amp;quot;_user_mail_notify('password_reset', \$account);&amp;quot;,
                &amp;quot;\\Drupal::service(\\Drupal\\user\\NotificationHandler::class)-&amp;gt;sendPasswordReset(\$account);&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, '_user_mail_notify')) {
            return null;
        }
        if (count($node-&amp;gt;args) !== 2) {
            return null;
        }
        if (!$node-&amp;gt;args[0] instanceof Arg || !$node-&amp;gt;args[1] instanceof Arg) {
            return null;
        }
        // Named arguments change the calling convention; skip for safety.
        if ($node-&amp;gt;args[0]-&amp;gt;name !== null || $node-&amp;gt;args[1]-&amp;gt;name !== null) {
            return null;
        }
        $opArg = $node-&amp;gt;args[0]-&amp;gt;value;
        if (!$opArg instanceof String_) {
            // The $op argument is not a plain string literal (e.g. a
            // variable); cannot determine which NotificationHandler method
            // to call without evaluating runtime data.
            return null;
        }
        if (!isset(self::OP_TO_METHOD[$opArg-&amp;gt;value])) {
            // Unknown / custom $op value; no equivalent method exists on
            // NotificationHandler.
            return null;
        }
        $method = self::OP_TO_METHOD[$opArg-&amp;gt;value];
        $service = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new ClassConstFetch(new FullyQualified('Drupal\\user\\NotificationHandler'), 'class'))],
        );
        return new MethodCall($service, $method, [$node-&amp;gt;args[1]]);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceUserMailNotifyRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace deprecated locale.module underscore functions with LocaleJs service calls</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-deprecated-locale-module-underscore-functions-with-3618358.php</link>
      <guid isPermaLink="false">01a03fe1-2952-7131-86db-0b66a4342edd</guid>
      <pubDate>Wed, 26 Aug 2026 21:01:55 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3618358"&gt;#3618358&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 deprecates the remaining &lt;code&gt;_locale_*&lt;/code&gt; underscore helper functions in &lt;code&gt;locale.module&lt;/code&gt; in favor of methods on the &lt;code&gt;Drupal\locale\LocaleJs&lt;/code&gt; service. This rule rewrites direct calls to &lt;code&gt;_locale_refresh_translations()&lt;/code&gt;, &lt;code&gt;_locale_invalidate_js()&lt;/code&gt;, &lt;code&gt;_locale_parse_js_file()&lt;/code&gt;, and &lt;code&gt;_locale_rebuild_js()&lt;/code&gt; into the equivalent &lt;code&gt;\Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;method()&lt;/code&gt; call, preserving arguments unchanged. Contrib code that rebuilds or invalidates JavaScript translation files keeps working after these globals are removed in Drupal 13.0.0.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;_locale_invalidate_js($langcode);
_locale_rebuild_js($langcode);
_locale_parse_js_file($filepath);
_locale_refresh_translations($langcodes, $lids);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;\Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;invalidate($langcode);
\Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;rebuild($langcode);
\Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;parseJsFile($filepath);
\Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;refreshTranslations($langcodes, $lids);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;_locale_refresh_configuration()&lt;/code&gt; and &lt;code&gt;_locale_strip_quotes()&lt;/code&gt; are also deprecated by this issue but have no replacement API, so calls to them are intentionally left untouched; callers must be manually inlined or removed. &lt;code&gt;LocaleJs::parseJsFile()&lt;/code&gt; and &lt;code&gt;LocaleJs::rebuild()&lt;/code&gt; are marked &lt;code&gt;@internal&lt;/code&gt; in core (public only for test coverage), so rewritten call sites still depend on an internal API surface that core may change without a deprecation cycle.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceLocaleJsUnderscoreFunctionsRector extends AbstractRector
{
    /**
     * @var array&amp;lt;string, string&amp;gt;
     */
    private const METHOD_MAP = [
        '_locale_refresh_translations' =&amp;gt; 'refreshTranslations',
        '_locale_invalidate_js' =&amp;gt; 'invalidate',
        '_locale_parse_js_file' =&amp;gt; 'parseJsFile',
        '_locale_rebuild_js' =&amp;gt; 'rebuild',
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated locale.module underscore functions with Drupal\locale\LocaleJs service calls.',
            [new CodeSample(
                '_locale_invalidate_js($langcode);',
                &amp;quot;\\Drupal::service(\\Drupal\\locale\\LocaleJs::class)-&amp;gt;invalidate(\$langcode);&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        if (!$node-&amp;gt;name instanceof Name) {
            // Skip dynamic calls, e.g. $fn(...).
            return null;
        }
        $functionName = $this-&amp;gt;getName($node-&amp;gt;name);
        if ($functionName === null || !isset(self::METHOD_MAP[$functionName])) {
            return null;
        }

        $service = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new ClassConstFetch(new FullyQualified('Drupal\\locale\\LocaleJs'), 'class'))],
        );

        return new MethodCall($service, self::METHOD_MAP[$functionName], $node-&amp;gt;args);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceLocaleJsUnderscoreFunctionsRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace deprecated locale.module global functions with class calls</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-deprecated-locale-module-global-functions-with-3616277.php</link>
      <guid isPermaLink="false">01a03b76-df84-749b-b3ff-430b1683299c</guid>
      <pubDate>Wed, 26 Aug 2026 00:27:21 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3616277"&gt;#3616277&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 deprecates several &lt;code&gt;locale.module&lt;/code&gt; global functions in favor of methods on new classes. &lt;code&gt;locale_string_is_safe()&lt;/code&gt; becomes the static &lt;code&gt;LocaleXss::stringIsSafe()&lt;/code&gt;; &lt;code&gt;locale_is_translatable()&lt;/code&gt;, &lt;code&gt;locale_translatable_language_list()&lt;/code&gt;, and &lt;code&gt;locale_js_translate()&lt;/code&gt; become instance methods on the &lt;code&gt;LocaleLanguages&lt;/code&gt; and &lt;code&gt;LocaleJs&lt;/code&gt; services, obtained via &lt;code&gt;\Drupal::service()&lt;/code&gt;. This rule rewrites direct calls to the old global functions so contrib and custom modules keep working after the functions are removed in Drupal 13.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$safe = locale_string_is_safe($string);
$translatable = locale_is_translatable($langcode);
$languages = locale_translatable_language_list();
$translation_file = locale_js_translate($files, $language_interface);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$safe = \Drupal\locale\LocaleXss::stringIsSafe($string);
$translatable = \Drupal::service(\Drupal\locale\LocaleLanguages::class)-&amp;gt;isTranslatable($langcode);
$languages = \Drupal::service(\Drupal\locale\LocaleLanguages::class)-&amp;gt;getTranslatableLanguages();
$translation_file = \Drupal::service(\Drupal\locale\LocaleJs::class)-&amp;gt;jsTranslate($files, $language_interface);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only covers the four functions with a direct 1:1 replacement (&lt;code&gt;locale_string_is_safe&lt;/code&gt;, &lt;code&gt;locale_is_translatable&lt;/code&gt;, &lt;code&gt;locale_translatable_language_list&lt;/code&gt;, &lt;code&gt;locale_js_translate&lt;/code&gt;). &lt;code&gt;locale_translation_use_remote_source()&lt;/code&gt; and &lt;code&gt;locale_translation_language_table()&lt;/code&gt; are also deprecated by this issue but have no direct replacement (inlined config check, or a class method meant to be referenced by name as a &lt;code&gt;#after_build&lt;/code&gt; callback), so they are intentionally left untouched; callers of those two must be migrated by hand.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceDeprecatedLocaleFunctionsRector extends AbstractRector
{
    /**
     * Functions replaced by a static method call.
     *
     * @var array&amp;lt;string, array{0: string, 1: string}&amp;gt;
     */
    private const STATIC_MAP = [
        'locale_string_is_safe' =&amp;gt; ['Drupal\locale\LocaleXss', 'stringIsSafe'],
    ];

    /**
     * Functions replaced by a call on a service obtained via \Drupal::service().
     *
     * @var array&amp;lt;string, array{0: string, 1: string}&amp;gt;
     */
    private const SERVICE_MAP = [
        'locale_is_translatable' =&amp;gt; ['Drupal\locale\LocaleLanguages', 'isTranslatable'],
        'locale_translatable_language_list' =&amp;gt; ['Drupal\locale\LocaleLanguages', 'getTranslatableLanguages'],
        'locale_js_translate' =&amp;gt; ['Drupal\locale\LocaleJs', 'jsTranslate'],
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated locale.module global functions with calls on their replacement classes.',
            [new CodeSample(
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
$safe = locale_string_is_safe($string);
$translatable = locale_is_translatable($langcode);
$languages = locale_translatable_language_list();
CODE_SAMPLE
                ,
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
$safe = \Drupal\locale\LocaleXss::stringIsSafe($string);
$translatable = \Drupal::service(\Drupal\locale\LocaleLanguages::class)-&amp;gt;isTranslatable($langcode);
$languages = \Drupal::service(\Drupal\locale\LocaleLanguages::class)-&amp;gt;getTranslatableLanguages();
CODE_SAMPLE
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }

        $functionName = $this-&amp;gt;getName($node-&amp;gt;name);
        if ($functionName === null) {
            return null;
        }

        if (isset(self::STATIC_MAP[$functionName])) {
            [$class, $method] = self::STATIC_MAP[$functionName];
            return new StaticCall(new FullyQualified($class), $method, $node-&amp;gt;args);
        }

        if (isset(self::SERVICE_MAP[$functionName])) {
            [$class, $method] = self::SERVICE_MAP[$functionName];
            $service = new StaticCall(
                new FullyQualified('Drupal'),
                'service',
                [new Arg(new ClassConstFetch(new FullyQualified($class), 'class'))],
            );
            return new MethodCall($service, $method, $node-&amp;gt;args);
        }

        return null;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceDeprecatedLocaleFunctionsRector::class]);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Add $memoryCache argument to UpdateRegistry instantiations</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/add-memorycache-argument-to-updateregistry-instantiations-3303751.php</link>
      <guid isPermaLink="false">01a0380c-430e-7307-8bb4-1b7bdf370122</guid>
      <pubDate>Tue, 25 Aug 2026 08:32:02 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3303751"&gt;#3303751&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal\Core\Update\UpdateRegistry::__construct() now takes an optional MemoryCacheInterface $memoryCache parameter inserted before the trailing $updateType parameter. Omitting it triggers an E_USER_DEPRECATED notice and will be a hard error in Drupal 12. This rule rewrites direct &lt;code&gt;new UpdateRegistry(...)&lt;/code&gt; calls (positional or named) to pass &lt;code&gt;\Drupal::service('cache.memory')&lt;/code&gt; in the new slot, correctly shifting an explicitly-passed $updateType.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new UpdateRegistry($root, $sitePath, $module_list, $keyValue, $theme_handler, 'post_update');
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new UpdateRegistry($root, $sitePath, $module_list, $keyValue, $theme_handler, \Drupal::service('cache.memory'), 'post_update');
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites direct &lt;code&gt;new UpdateRegistry(...)&lt;/code&gt; instantiations; code that obtains the service via &lt;code&gt;\Drupal::service('update.post_update_registry')&lt;/code&gt; or dependency injection needs no change since core's service definition already passes the new argument. Calls using &lt;code&gt;...$args&lt;/code&gt; spread/unpacking are skipped because the argument position cannot be determined statically; these need manual review.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\New_;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Identifier;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use PhpParser\Node\VariadicPlaceholder;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class AddUpdateRegistryMemoryCacheArgumentRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Add the $memoryCache constructor argument to Drupal\Core\Update\UpdateRegistry instantiations.',
            [new CodeSample(
                'new UpdateRegistry($root, $sitePath, $module_list, $keyValue, $theme_handler, \'post_update\');',
                &amp;quot;new UpdateRegistry(\$root, \$sitePath, \$module_list, \$keyValue, \$theme_handler, \\Drupal::service('cache.memory'), 'post_update');&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [New_::class];
    }

    /** @param New_ $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof New_) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;class, 'Drupal\\Core\\Update\\UpdateRegistry')) {
            return null;
        }

        $args = $node-&amp;gt;args;

        // Bail on unpacked/spread args (`...$args`): position cannot be determined safely.
        foreach ($args as $arg) {
            if ($arg instanceof VariadicPlaceholder) {
                return null;
            }
        }

        $memoryCacheCall = new StaticCall(new FullyQualified('Drupal'), 'service', [new Arg(new String_('cache.memory'))]);

        $hasNamedArgs = false;
        foreach ($args as $arg) {
            if ($arg-&amp;gt;name !== null) {
                $hasNamedArgs = true;
                if ($this-&amp;gt;isName($arg-&amp;gt;name, 'memoryCache')) {
                    // Already migrated.
                    return null;
                }
            }
        }

        if ($hasNamedArgs) {
            $node-&amp;gt;args[] = new Arg($memoryCacheCall, false, false, [], new Identifier('memoryCache'));
            return $node;
        }

        $argCount = count($args);

        // Fewer than 5 positional args is not a valid call to this constructor
        // ($root, $sitePath, $module_list, $keyValue, $theme_handler are all required); skip.
        if ($argCount &amp;lt; 5) {
            return null;
        }

        // 7+ positional args already include $memoryCache; nothing to do.
        if ($argCount &amp;gt;= 7) {
            return null;
        }

        $memoryCacheArg = new Arg($memoryCacheCall);

        if ($argCount === 5) {
            // No $updateType passed: append $memoryCache as the 6th argument.
            $node-&amp;gt;args[] = $memoryCacheArg;
            return $node;
        }

        // $argCount === 6: an explicit $updateType was passed positionally in the old
        // signature's 6th slot. Insert $memoryCache before it, shifting $updateType to 7th.
        array_splice($node-&amp;gt;args, 5, 0, [$memoryCacheArg]);
        return $node;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([AddUpdateRegistryMemoryCacheArgumentRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace deprecated installer-specific extension list classes</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-deprecated-installer-specific-extension-list-classes-2934063.php</link>
      <guid isPermaLink="false">01a00d1c-9edc-729a-a105-b91e133a521d</guid>
      <pubDate>Mon, 17 Aug 2026 00:26:14 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/2934063"&gt;#2934063&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal\Core\Installer\InstallerModuleExtensionList and InstallerThemeExtensionList are now empty backward-compatibility shims around Drupal\Core\Extension\ModuleExtensionList and ThemeExtensionList; the installer no longer swaps them into the container. This rule rewrites type hints, property types, &lt;code&gt;new&lt;/code&gt; calls, &lt;code&gt;instanceof&lt;/code&gt; checks, and &lt;code&gt;use&lt;/code&gt; imports from the deprecated installer classes to their base-class equivalents, which provide identical behavior.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\Core\Installer\InstallerModuleExtensionList;

class MyService {
  public function __construct(InstallerModuleExtensionList $module_list) {}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\Core\Extension\ModuleExtensionList;

class MyService {
  public function __construct(ModuleExtensionList $module_list) {}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites the class references. It does not touch calls to the now-deprecated &lt;code&gt;ExtensionList::setPathname()&lt;/code&gt; method that some contrib code may still call on the resulting object; that method has no direct replacement and is out of scope for this rule.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use Rector\Config\RectorConfig;
use Rector\Renaming\Rector\Name\RenameClassRector;

return RectorConfig::configure()
    -&amp;gt;withConfiguredRule(RenameClassRector::class, [
        'Drupal\\Core\\Installer\\InstallerModuleExtensionList' =&amp;gt; 'Drupal\\Core\\Extension\\ModuleExtensionList',
        'Drupal\\Core\\Installer\\InstallerThemeExtensionList' =&amp;gt; 'Drupal\\Core\\Extension\\ThemeExtensionList',
    ]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Rename NavigationShortcutsBlock to ShortcutNavigationBlock</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/rename-navigationshortcutsblock-to-shortcutnavigationblock-3581816.php</link>
      <guid isPermaLink="false">01a009a5-33c6-71fa-ab0a-54d6af595851</guid>
      <pubDate>Sun, 16 Aug 2026 08:16:56 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3581816"&gt;#3581816&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal core moved the shortcuts navigation block plugin class out of the Navigation module and into the Shortcut module. &lt;code&gt;Drupal\navigation\Plugin\Block\NavigationShortcutsBlock&lt;/code&gt; is deprecated in drupal:11.5.0 and removed in drupal:12.0.0 in favor of &lt;code&gt;Drupal\shortcut\Plugin\Block\ShortcutNavigationBlock&lt;/code&gt;, which has the same constructor signature. This rule rewrites &lt;code&gt;use&lt;/code&gt;, &lt;code&gt;extends&lt;/code&gt;, &lt;code&gt;instanceof&lt;/code&gt;, &lt;code&gt;new&lt;/code&gt;, and type-hint references from the old class to the new one.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\navigation\Plugin\Block\NavigationShortcutsBlock;

class MyCustomBlock extends NavigationShortcutsBlock {

}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\shortcut\Plugin\Block\ShortcutNavigationBlock;

class MyCustomBlock extends ShortcutNavigationBlock {

}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Does not cover the related internal rename of &lt;code&gt;Drupal\navigation\ShortcutLazyBuilder&lt;/code&gt; to &lt;code&gt;Drupal\shortcut\ShortcutNavigationLazyBuilder&lt;/code&gt;, since that class carries no &lt;code&gt;@deprecated&lt;/code&gt; marker or back-compat shim in core (it is &lt;code&gt;@internal&lt;/code&gt; and was removed outright), so there is no documented deprecation to target.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use Rector\Config\RectorConfig;
use Rector\Renaming\Rector\Name\RenameClassRector;

return RectorConfig::configure()
    -&amp;gt;withConfiguredRule(RenameClassRector::class, [
        'Drupal\navigation\Plugin\Block\NavigationShortcutsBlock' =&amp;gt; 'Drupal\shortcut\Plugin\Block\ShortcutNavigationBlock',
    ]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace options_allowed_values() with the OptionsAllowedValuesInterface service</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-options-allowed-values-with-the-2171397.php</link>
      <guid isPermaLink="false">019fee3e-98ed-7675-8a3d-ad4926a3f858</guid>
      <pubDate>Tue, 11 Aug 2026 00:35:07 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/2171397"&gt;#2171397&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal core deprecated the global function &lt;code&gt;options_allowed_values()&lt;/code&gt; and moved its logic into the &lt;code&gt;Drupal\options\OptionsAllowedValuesInterface&lt;/code&gt; service (&lt;code&gt;getAllowedValues()&lt;/code&gt; method). This rule rewrites direct calls to the old global function into a call on the new service, preserving the original arguments (the field storage definition and optional entity), so contrib and custom code calling it directly keeps working after the function is removed.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$values = options_allowed_values($definition, $entity);
// or without an entity:
$values = options_allowed_values($definition);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$values = \Drupal::service(\Drupal\options\OptionsAllowedValuesInterface::class)-&amp;gt;getAllowedValues($definition, $entity);
// or without an entity:
$values = \Drupal::service(\Drupal\options\OptionsAllowedValuesInterface::class)-&amp;gt;getAllowedValues($definition);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites calls with 1 or 2 positional arguments (the documented signature of &lt;code&gt;options_allowed_values()&lt;/code&gt;); calls with other arg counts, named arguments, or first-class callable syntax (&lt;code&gt;options_allowed_values(...)&lt;/code&gt;) are left untouched for manual review. Does not inject the service via dependency injection; it always uses &lt;code&gt;\Drupal::service()&lt;/code&gt;, which is safe everywhere but not idiomatic inside injectable classes such as plugins or services (manual follow-up recommended there).&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceOptionsAllowedValuesFunctionRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace options_allowed_values() with the options.allowed_values service.',
            [new CodeSample(
                '$values = options_allowed_values($definition, $entity);',
                &amp;quot;\$values = \\Drupal::service(\\Drupal\\options\\OptionsAllowedValuesInterface::class)-&amp;gt;getAllowedValues(\$definition, \$entity);&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        if ($node-&amp;gt;isFirstClassCallable()) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'options_allowed_values')) {
            return null;
        }
        if (count($node-&amp;gt;args) &amp;lt; 1 || count($node-&amp;gt;args) &amp;gt; 2) {
            return null;
        }

        $interfaceClassConst = new ClassConstFetch(
            new FullyQualified('Drupal\options\OptionsAllowedValuesInterface'),
            'class',
        );

        $service = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg($interfaceClassConst)],
        );

        return new MethodCall($service, 'getAllowedValues', $node-&amp;gt;args);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceOptionsAllowedValuesFunctionRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Add $yaml_cache_collector argument to Local Action/Task/MenuLink manager constructors</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/add-yaml-cache-collector-argument-to-local-action-task-3593485.php</link>
      <guid isPermaLink="false">019fd3ab-9e6f-74ed-a76a-5b13c2bb3aaa</guid>
      <pubDate>Wed, 05 Aug 2026 20:44:27 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3593485"&gt;#3593485&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal 11.5 adds an optional &lt;code&gt;$yaml_cache_collector&lt;/code&gt; constructor argument to &lt;code&gt;LocalActionManager&lt;/code&gt;, &lt;code&gt;LocalTaskManager&lt;/code&gt;, and &lt;code&gt;MenuLinkManager&lt;/code&gt; so they can use &lt;code&gt;YamlCacheCollectorDiscovery&lt;/code&gt;. Omitting it triggers a deprecation warning and it becomes required in Drupal 12. This rule appends the matching &lt;code&gt;\Drupal::service(...)&lt;/code&gt; call as the trailing argument on direct &lt;code&gt;new&lt;/code&gt; instantiations of these three classes and on &lt;code&gt;parent::__construct()&lt;/code&gt; calls inside subclasses that extend them, only when the exact prior positional argument count is present.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new \Drupal\Core\Menu\LocalActionManager($argument_resolver, $request_stack, $route_match, $route_provider, $module_handler, $cache_backend, $language_manager, $access_manager, $account);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new \Drupal\Core\Menu\LocalActionManager($argument_resolver, $request_stack, $route_match, $route_provider, $module_handler, $cache_backend, $language_manager, $access_manager, $account, \Drupal::service('local_action.yaml_cache_collector'));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites direct instantiations and &lt;code&gt;parent::__construct()&lt;/code&gt; calls whose positional argument count exactly matches the pre-change signature (9 for &lt;code&gt;LocalActionManager&lt;/code&gt;/&lt;code&gt;LocalTaskManager&lt;/code&gt;, 3 for &lt;code&gt;MenuLinkManager&lt;/code&gt;); calls with too few arguments, &lt;code&gt;...$args&lt;/code&gt; unpacking, or first-class callable syntax are left untouched for manual review. Subclasses that extend an intermediate class rather than the manager directly are not matched, since the intermediate class's constructor signature is not verified.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\New_;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Identifier;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use PhpParser\Node\Stmt\Class_;
use PhpParser\Node\Stmt\ClassMethod;
use PhpParser\Node\Stmt\Expression;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class AddYamlCacheCollectorArgumentToMenuManagersRector extends AbstractRector
{
    /**
     * Maps the manager FQCN to [service id for the new arg, position of the new (last) arg].
     * All three manager constructors name the new parameter `$yaml_cache_collector`.
     *
     * @var array&amp;lt;string, array{0: string, 1: int}&amp;gt;
     */
    private const MANAGERS = [
        'Drupal\Core\Menu\LocalActionManager' =&amp;gt; ['local_action.yaml_cache_collector', 9],
        'Drupal\Core\Menu\LocalTaskManager' =&amp;gt; ['local_task.yaml_cache_collector', 9],
        'Drupal\Core\Menu\MenuLinkManager' =&amp;gt; ['menu_link.yaml_cache_collector', 3],
    ];

    private const ARGUMENT_NAME = 'yaml_cache_collector';

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Add the new $yaml_cache_collector constructor argument to LocalActionManager, LocalTaskManager, and MenuLinkManager instantiations and to parent::__construct() calls in subclasses, since omitting it is deprecated.',
            [new CodeSample(
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
new \Drupal\Core\Menu\LocalActionManager($argument_resolver, $request_stack, $route_match, $route_provider, $module_handler, $cache_backend, $language_manager, $access_manager, $account);
CODE_SAMPLE
                ,
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
new \Drupal\Core\Menu\LocalActionManager($argument_resolver, $request_stack, $route_match, $route_provider, $module_handler, $cache_backend, $language_manager, $access_manager, $account, \Drupal::service('local_action.yaml_cache_collector'));
CODE_SAMPLE
            )]
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [New_::class, Class_::class];
    }

    /**
     * @param New_|Class_ $node
     */
    public function refactor(Node $node): ?Node
    {
        if ($node instanceof New_) {
            return $this-&amp;gt;refactorNew($node);
        }

        if ($node instanceof Class_) {
            return $this-&amp;gt;refactorSubclassConstructor($node);
        }

        return null;
    }

    private function refactorNew(New_ $node): ?Node
    {
        foreach (self::MANAGERS as $fqcn =&amp;gt; [$serviceId, $position]) {
            if (!$this-&amp;gt;isName($node-&amp;gt;class, $fqcn)) {
                continue;
            }

            return $this-&amp;gt;appendArgumentIfMissing($node, $position, $serviceId) ? $node : null;
        }

        return null;
    }

    private function refactorSubclassConstructor(Class_ $node): ?Node
    {
        if (!$node-&amp;gt;extends instanceof Node\Name) {
            return null;
        }

        $target = null;
        foreach (self::MANAGERS as $fqcn =&amp;gt; $config) {
            if ($this-&amp;gt;isName($node-&amp;gt;extends, $fqcn)) {
                $target = $config;
                break;
            }
        }

        if ($target === null) {
            return null;
        }
        [$serviceId, $position] = $target;

        $constructMethod = $node-&amp;gt;getMethod('__construct');
        if (!$constructMethod instanceof ClassMethod || $constructMethod-&amp;gt;stmts === null) {
            return null;
        }

        $hasChanged = false;
        foreach ($constructMethod-&amp;gt;stmts as $stmt) {
            if (!$stmt instanceof Expression) {
                continue;
            }
            if (!$stmt-&amp;gt;expr instanceof StaticCall) {
                continue;
            }
            $staticCall = $stmt-&amp;gt;expr;
            if (!$this-&amp;gt;isName($staticCall-&amp;gt;class, 'parent')) {
                continue;
            }
            if (!$this-&amp;gt;isName($staticCall-&amp;gt;name, '__construct')) {
                continue;
            }
            if ($this-&amp;gt;appendArgumentIfMissing($staticCall, $position, $serviceId)) {
                $hasChanged = true;
            }
        }

        return $hasChanged ? $node : null;
    }

    /**
     * @param New_|StaticCall $call
     */
    private function appendArgumentIfMissing($call, int $position, string $serviceId): bool
    {
        if ($call-&amp;gt;isFirstClassCallable()) {
            return false;
        }

        $hasNamedArgs = false;
        foreach ($call-&amp;gt;args as $arg) {
            if (!$arg instanceof Arg) {
                // Unpacked/variadic args make position counting unreliable.
                return false;
            }
            if ($arg-&amp;gt;name !== null) {
                $hasNamedArgs = true;
                if ($this-&amp;gt;isName($arg-&amp;gt;name, self::ARGUMENT_NAME)) {
                    // Argument already present as a named argument.
                    return false;
                }
            }
        }

        $newArgValue = new StaticCall(new FullyQualified('Drupal'), 'service', [new Arg(new String_($serviceId))]);
        $newArg = new Arg($newArgValue);

        if ($hasNamedArgs) {
            $newArg-&amp;gt;name = new Identifier(self::ARGUMENT_NAME);
            $call-&amp;gt;args[] = $newArg;
            return true;
        }

        // Only append when exactly the required positional arguments are present;
        // anything else (too few, or the new argument already present) is left alone.
        if (count($call-&amp;gt;args) !== $position) {
            return false;
        }

        $call-&amp;gt;args[] = $newArg;
        return true;
    }
}

return RectorConfig::configure()
    -&amp;gt;withRules([AddYamlCacheCollectorArgumentToMenuManagersRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Promote defaults._title to top-level title on #[Route] attributes</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/promote-defaults-title-to-top-level-title-on-route-3607968.php</link>
      <guid isPermaLink="false">019fbad8-6428-744c-b0f7-5b695911fbf5</guid>
      <pubDate>Sat, 01 Aug 2026 01:02:51 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3607968"&gt;#3607968&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal's new &lt;code&gt;Drupal\Core\Routing\Attribute\Route&lt;/code&gt; attribute extends Symfony's &lt;code&gt;#[Route]&lt;/code&gt; with a top-level &lt;code&gt;title&lt;/code&gt; property, replacing the nested &lt;code&gt;defaults: ['_title' =&amp;gt; ...]&lt;/code&gt; convention. This rule finds &lt;code&gt;#[Route]&lt;/code&gt; attributes (from &lt;code&gt;Symfony\Component\Routing\Attribute\Route&lt;/code&gt;, including aliased imports) whose &lt;code&gt;defaults&lt;/code&gt; array contains a &lt;code&gt;_title&lt;/code&gt; key, moves that value to a &lt;code&gt;title:&lt;/code&gt; named argument placed after &lt;code&gt;name:&lt;/code&gt;, drops &lt;code&gt;_title&lt;/code&gt; from &lt;code&gt;defaults&lt;/code&gt; (removing the argument entirely if it becomes empty), and repoints the attribute at Drupal's &lt;code&gt;Route&lt;/code&gt; subclass.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Symfony\Component\Routing\Attribute\Route;

#[Route(
  path: '/admin/config/system/site-information',
  name: 'system.site_information_settings',
  requirements: [
    '_permission' =&amp;gt; 'administer site configuration',
  ],
  defaults: ['_title' =&amp;gt; new TranslatableMarkup('Basic site settings')],
)]
class SiteInformationForm extends ConfigFormBase {
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\Core\Routing\Attribute\Route;

#[Route(
  path: '/admin/config/system/site-information',
  name: 'system.site_information_settings',
  title: new TranslatableMarkup('Basic site settings'),
  requirements: [
    '_permission' =&amp;gt; 'administer site configuration',
  ],
)]
class SiteInformationForm extends ConfigFormBase {
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;The new class reference is written as a fully-qualified name (&lt;code&gt;\Drupal\Core\Routing\Attribute\Route&lt;/code&gt;) rather than added as a clean &lt;code&gt;use&lt;/code&gt; import, because safely rewriting imports without colliding with other, unconverted &lt;code&gt;Route&lt;/code&gt; attributes in the same file (e.g. ones without &lt;code&gt;_title&lt;/code&gt;, still bound to the short name &lt;code&gt;Route&lt;/code&gt; via the old import) is out of scope; the emitted code is valid PHP either way. The rule only handles the &lt;code&gt;_title&lt;/code&gt; key; it does not touch &lt;code&gt;_title_callback&lt;/code&gt;/&lt;code&gt;_title_arguments&lt;/code&gt;/&lt;code&gt;_title_context&lt;/code&gt; defaults, and it skips attributes where &lt;code&gt;defaults&lt;/code&gt; is not a literal array (e.g. a variable) or where a &lt;code&gt;title:&lt;/code&gt; argument is already present.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Attribute;
use PhpParser\Node\Expr\Array_;
use PhpParser\Node\Identifier;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class PromoteRouteAttributeTitleRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Promote the &amp;quot;_title&amp;quot; route default to the top-level &amp;quot;title&amp;quot; property on #[Route] attributes, switching to Drupal\'s Route attribute subclass.',
            [new CodeSample(
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
use Symfony\Component\Routing\Attribute\Route;

#[Route(
    path: '/foo',
    name: 'foo.route',
    defaults: ['_title' =&amp;gt; 'Foo'],
)]
CODE_SAMPLE
                ,
                &amp;lt;&amp;lt;&amp;lt;'CODE_SAMPLE'
use Drupal\Core\Routing\Attribute\Route;

#[Route(
    path: '/foo',
    name: 'foo.route',
    title: 'Foo',
)]
CODE_SAMPLE
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [Attribute::class];
    }

    /** @param Attribute $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof Attribute) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'Symfony\Component\Routing\Attribute\Route')) {
            return null;
        }

        $defaultsArgIndex = null;
        $titleItemIndex = null;
        $titleValue = null;
        $hasTitleArg = false;
        foreach ($node-&amp;gt;args as $i =&amp;gt; $arg) {
            if ($arg-&amp;gt;name === null) {
                continue;
            }
            if ($arg-&amp;gt;name-&amp;gt;toString() === 'title') {
                $hasTitleArg = true;
                continue;
            }
            if ($arg-&amp;gt;name-&amp;gt;toString() !== 'defaults') {
                continue;
            }
            if (!$arg-&amp;gt;value instanceof Array_) {
                return null;
            }
            foreach ($arg-&amp;gt;value-&amp;gt;items as $j =&amp;gt; $item) {
                if ($item === null || $item-&amp;gt;key === null) {
                    continue;
                }
                if (!$item-&amp;gt;key instanceof String_ || $item-&amp;gt;key-&amp;gt;value !== '_title') {
                    continue;
                }
                $defaultsArgIndex = $i;
                $titleItemIndex = $j;
                $titleValue = $item-&amp;gt;value;
            }
        }

        if ($hasTitleArg || $defaultsArgIndex === null || $titleItemIndex === null) {
            return null;
        }

        // Remove the `_title` entry from defaults.
        $defaultsArg = $node-&amp;gt;args[$defaultsArgIndex];
        assert($defaultsArg-&amp;gt;value instanceof Array_);
        unset($defaultsArg-&amp;gt;value-&amp;gt;items[$titleItemIndex]);
        $defaultsArg-&amp;gt;value-&amp;gt;items = array_values($defaultsArg-&amp;gt;value-&amp;gt;items);

        // Drop the whole `defaults` argument if it is now empty.
        if ($defaultsArg-&amp;gt;value-&amp;gt;items === []) {
            unset($node-&amp;gt;args[$defaultsArgIndex]);
        }
        $node-&amp;gt;args = array_values($node-&amp;gt;args);

        // Insert a `title:` named argument, right after `name:` when present, otherwise first.
        $titleArg = new Arg($titleValue, false, false, [], new Identifier('title'));
        $insertAt = 0;
        foreach ($node-&amp;gt;args as $i =&amp;gt; $arg) {
            if ($arg-&amp;gt;name !== null &amp;amp;&amp;amp; $arg-&amp;gt;name-&amp;gt;toString() === 'name') {
                $insertAt = $i + 1;
                break;
            }
        }
        array_splice($node-&amp;gt;args, $insertAt, 0, [$titleArg]);

        // Point the attribute at Drupal's Route subclass instead of Symfony's.
        $node-&amp;gt;name = new FullyQualified('Drupal\Core\Routing\Attribute\Route');

        return $node;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([PromoteRouteAttributeTitleRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace FileStorageFactory::getSync() with the config.storage.sync service</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-filestoragefactory-getsync-with-the-config-storage-2951046.php</link>
      <guid isPermaLink="false">019fa47e-60f6-706b-9b5f-e64fbcc01c9c</guid>
      <pubDate>Mon, 27 Jul 2026 16:52:53 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/2951046"&gt;#2951046&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;Drupal\Core\Config\FileStorageFactory::getSync()&lt;/code&gt; is deprecated in favor of the &lt;code&gt;config.storage.sync&lt;/code&gt; service, which now wraps the sync directory in an &lt;code&gt;AutoloadingStorage&lt;/code&gt; decorator so PHP constants and enums referenced in YAML config can be resolved even for not-yet-installed extensions. This rule rewrites zero-argument calls to the static factory method into a call to &lt;code&gt;\Drupal::service('config.storage.sync')&lt;/code&gt;, which contrib code commonly uses to read or compare staged configuration.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$sync = \Drupal\Core\Config\FileStorageFactory::getSync();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$sync = \Drupal::service('config.storage.sync');
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites zero-argument calls to &lt;code&gt;getSync()&lt;/code&gt;; the method takes no parameters upstream, so this is not a real limitation. Does not rewrite &lt;code&gt;new FileStorageFactory()&lt;/code&gt; construction, which is also deprecated but rare in practice since the class exposes no other usable API once instantiated.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceFileStorageFactoryGetSyncRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace FileStorageFactory::getSync() with the config.storage.sync service.',
            [new CodeSample(
                '$sync = \Drupal\Core\Config\FileStorageFactory::getSync();',
                &amp;quot;\$sync = \\Drupal::service('config.storage.sync');&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [StaticCall::class];
    }

    /** @param StaticCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof StaticCall) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;class, 'Drupal\\Core\\Config\\FileStorageFactory')) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'getSync')) {
            return null;
        }
        if (count($node-&amp;gt;args) !== 0) {
            return null;
        }
        return new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new String_('config.storage.sync'))],
        );
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceFileStorageFactoryGetSyncRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Rename UserSearch to SearchUser from search_user module</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/rename-usersearch-to-searchuser-from-search-user-module-3588379.php</link>
      <guid isPermaLink="false">019f9cdb-27c8-711b-966e-276537212c5f</guid>
      <pubDate>Sun, 26 Jul 2026 05:17:16 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3588379"&gt;#3588379&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Drupal\user\Plugin\Search\UserSearch was moved into a new search_user submodule and renamed to Drupal\search_user\Plugin\Search\SearchUser, deprecated in drupal:11.5.0 and removed in drupal:12.0.0. The constructor signature is unchanged, so this rule uses Rector's built-in RenameClassRector to rewrite &lt;code&gt;use&lt;/code&gt;, &lt;code&gt;extends&lt;/code&gt;, &lt;code&gt;instanceof&lt;/code&gt;, and &lt;code&gt;new&lt;/code&gt; references from the old class to the new one across contrib and custom search plugins.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\user\Plugin\Search\UserSearch;

class MyPlugin extends UserSearch {
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;use Drupal\search_user\Plugin\Search\SearchUser;

class MyPlugin extends SearchUser {
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Uses require the search_user submodule to be enabled alongside search; RenameClassRector only rewrites the PHP-level reference, so module dependency declarations (info.yml) must still be updated manually if a contrib module extends this class.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use Rector\Config\RectorConfig;
use Rector\Renaming\Rector\Name\RenameClassRector;

return RectorConfig::configure()
    -&amp;gt;withConfiguredRule(RenameClassRector::class, [
        'Drupal\\user\\Plugin\\Search\\UserSearch' =&amp;gt; 'Drupal\\search_user\\Plugin\\Search\\SearchUser',
    ]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace StubPDO::class with \PDO::class in tests</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-stubpdo-class-with-pdo-class-in-tests-3585476.php</link>
      <guid isPermaLink="false">019f9a1e-992c-7713-aa51-ac2e959e4c24</guid>
      <pubDate>Sat, 25 Jul 2026 16:32:04 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3585476"&gt;#3585476&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;Drupal\Tests\Core\Database\Stub\StubPDO&lt;/code&gt; is deprecated with no replacement: it existed only because older PHPUnit could not mock &lt;code&gt;\PDO&lt;/code&gt; directly, so core wrapped it in a no-op subclass. Now that PHPUnit can stub &lt;code&gt;\PDO&lt;/code&gt; itself, tests that pass &lt;code&gt;StubPDO::class&lt;/code&gt; to &lt;code&gt;createStub()&lt;/code&gt;/&lt;code&gt;createMock()&lt;/code&gt;/&lt;code&gt;getMockBuilder()&lt;/code&gt; should reference &lt;code&gt;\PDO::class&lt;/code&gt; directly. This rule rewrites the &lt;code&gt;::class&lt;/code&gt; constant fetch only, since &lt;code&gt;StubPDO&lt;/code&gt;'s constructor is a no-op while &lt;code&gt;\PDO&lt;/code&gt;'s is not: rewriting &lt;code&gt;new StubPDO()&lt;/code&gt;, &lt;code&gt;extends StubPDO&lt;/code&gt;, or &lt;code&gt;instanceof StubPDO&lt;/code&gt; would change runtime behavior, so those forms are intentionally left untouched.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$mock_pdo = $this-&amp;gt;createStub(\Drupal\Tests\Core\Database\Stub\StubPDO::class);
$connection = new StubConnection($mock_pdo, []);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$mock_pdo = $this-&amp;gt;createStub(\PDO::class);
$connection = new StubConnection($mock_pdo, []);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only rewrites the &lt;code&gt;::class&lt;/code&gt; constant fetch. Direct instantiation (&lt;code&gt;new StubPDO()&lt;/code&gt;), &lt;code&gt;extends StubPDO&lt;/code&gt;, and &lt;code&gt;instanceof StubPDO&lt;/code&gt; are left untouched because &lt;code&gt;StubPDO&lt;/code&gt;'s constructor is a no-op while &lt;code&gt;\PDO&lt;/code&gt;'s real constructor requires connection arguments; blindly renaming those forms could produce a fatal error. Leftover unused &lt;code&gt;use Drupal\Tests\Core\Database\Stub\StubPDO;&lt;/code&gt; imports are not removed.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceStubPdoClassConstantRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace \Drupal\Tests\Core\Database\Stub\StubPDO::class with \PDO::class.',
            [new CodeSample(
                '$mock = $this-&amp;gt;createStub(\Drupal\Tests\Core\Database\Stub\StubPDO::class);',
                '$mock = $this-&amp;gt;createStub(\PDO::class);',
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [ClassConstFetch::class];
    }

    /** @param ClassConstFetch $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof ClassConstFetch) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'class')) {
            return null;
        }
        if (!$node-&amp;gt;class instanceof Node\Name) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;class, 'Drupal\\Tests\\Core\\Database\\Stub\\StubPDO')) {
            return null;
        }

        return new ClassConstFetch(new FullyQualified('PDO'), 'class');
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceStubPdoClassConstantRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace module_set_weight() and module_config_sort() with ModuleWeight service</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-module-set-weight-and-module-config-sort-with-3595652.php</link>
      <guid isPermaLink="false">019f7ed7-9155-7340-8211-75776a4b1521</guid>
      <pubDate>Mon, 20 Jul 2026 09:24:44 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3595652"&gt;#3595652&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Rewrites calls to the deprecated global functions &lt;code&gt;module_set_weight()&lt;/code&gt; and &lt;code&gt;module_config_sort()&lt;/code&gt; into equivalent calls on the new &lt;code&gt;Drupal\Core\Extension\ModuleWeight&lt;/code&gt; service. Both functions in &lt;code&gt;module.inc&lt;/code&gt; were deprecated in Drupal 11.5.0 and will be removed in 13.0.0. Contrib modules commonly call them in &lt;code&gt;hook_install()&lt;/code&gt; implementations and test setup, so automated migration saves significant manual effort.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;module_set_weight('views', 10);
$sorted = module_config_sort($modules);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;\Drupal::service(\Drupal\Core\Extension\ModuleWeight::class)-&amp;gt;set('views', 10);
$sorted = \Drupal::service(\Drupal\Core\Extension\ModuleWeight::class)-&amp;gt;sort($modules);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Identifier;
use PhpParser\Node\Name\FullyQualified;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class ReplaceModuleIncFunctionsWithModuleWeightRector extends AbstractRector
{
    private const FUNCTION_TO_METHOD = [
        'module_set_weight' =&amp;gt; 'set',
        'module_config_sort' =&amp;gt; 'sort',
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated module_set_weight() and module_config_sort() with \Drupal::service(ModuleWeight::class)-&amp;gt;set/sort().',
            [
                new CodeSample(
                    'module_set_weight(\'views\', 10);',
                    '\Drupal::service(\Drupal\Core\Extension\ModuleWeight::class)-&amp;gt;set(\'views\', 10);'
                ),
                new CodeSample(
                    '$sorted = module_config_sort($modules);',
                    '$sorted = \Drupal::service(\Drupal\Core\Extension\ModuleWeight::class)-&amp;gt;sort($modules);'
                ),
            ]
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }

        $method = null;
        foreach (self::FUNCTION_TO_METHOD as $funcName =&amp;gt; $methodName) {
            if ($this-&amp;gt;isName($node-&amp;gt;name, $funcName)) {
                $method = $methodName;
                break;
            }
        }
        if ($method === null) {
            return null;
        }

        $moduleWeightClass = new ClassConstFetch(
            new FullyQualified('Drupal\\Core\\Extension\\ModuleWeight'),
            new Identifier('class')
        );
        $serviceCall = new StaticCall(
            new FullyQualified('Drupal'),
            new Identifier('service'),
            [new Arg($moduleWeightClass)]
        );

        return new MethodCall($serviceCall, new Identifier($method), $node-&amp;gt;args);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([ReplaceModuleIncFunctionsWithModuleWeightRector::class]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace Htmx::triggerAfterSettleHeader() and triggerAfterSwapHeader() with triggerHeader()</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-htmx-triggeraftersettleheader-and-3607711.php</link>
      <guid isPermaLink="false">019f7017-9958-7209-990b-56e0a19b5d28</guid>
      <pubDate>Fri, 17 Jul 2026 12:40:22 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3607711"&gt;#3607711&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Renames the deprecated &lt;code&gt;triggerAfterSettleHeader()&lt;/code&gt; and &lt;code&gt;triggerAfterSwapHeader()&lt;/code&gt; methods on &lt;code&gt;Drupal\Core\Htmx\Htmx&lt;/code&gt; to &lt;code&gt;triggerHeader()&lt;/code&gt;. Both methods were deprecated in Drupal 11.5.0 in preparation for the htmx v4 upgrade and will be removed in 12.0.0. Contrib modules using the Htmx builder API can be upgraded automatically.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$htmx-&amp;gt;triggerAfterSettleHeader('myEvent');
$htmx-&amp;gt;triggerAfterSwapHeader(['key' =&amp;gt; 'value']);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$htmx-&amp;gt;triggerHeader('myEvent');
$htmx-&amp;gt;triggerHeader(['key' =&amp;gt; 'value']);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Other deprecated Htmx methods (&lt;code&gt;disabledElt&lt;/code&gt;, &lt;code&gt;disinherit&lt;/code&gt;, &lt;code&gt;ext&lt;/code&gt;, &lt;code&gt;history&lt;/code&gt;, &lt;code&gt;inherit&lt;/code&gt;, &lt;code&gt;params&lt;/code&gt;, &lt;code&gt;prompt&lt;/code&gt;, &lt;code&gt;request&lt;/code&gt;) and &lt;code&gt;HtmxRequestInfoTrait&lt;/code&gt; methods (&lt;code&gt;getHtmxPrompt&lt;/code&gt;, &lt;code&gt;getHtmxTrigger&lt;/code&gt;) have no direct programmatic replacement and cannot be automatically rewritten; those require manual review of the change record at https://www.drupal.org/node/3583674.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php

declare(strict_types=1);

use Rector\Config\RectorConfig;
use Rector\Renaming\Rector\MethodCall\RenameMethodRector;
use Rector\Renaming\ValueObject\MethodCallRename;

return RectorConfig::configure()
    -&amp;gt;withConfiguredRule(RenameMethodRector::class, [
        new MethodCallRename('Drupal\Core\Htmx\Htmx', 'triggerAfterSettleHeader', 'triggerHeader'),
        new MethodCallRename('Drupal\Core\Htmx\Htmx', 'triggerAfterSwapHeader', 'triggerHeader'),
    ]);

&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Add missing $imageDerivativeUtilities argument to ImageFormatter constructor calls</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/add-missing-imagederivativeutilities-argument-to-3609124.php</link>
      <guid isPermaLink="false">019f41c5-0ffd-7313-a1e2-917894fd15c7</guid>
      <pubDate>Wed, 08 Jul 2026 12:47:41 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3609124"&gt;#3609124&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.4.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Calling &lt;code&gt;ImageFormatter&lt;/code&gt;, &lt;code&gt;ImageUrlFormatter&lt;/code&gt;, or &lt;code&gt;MediaThumbnailFormatter&lt;/code&gt; constructors without the &lt;code&gt;$imageDerivativeUtilities&lt;/code&gt; argument is deprecated in &lt;code&gt;drupal:11.4.0&lt;/code&gt; and will be required in &lt;code&gt;drupal:12.0.0&lt;/code&gt;. This rule detects direct &lt;code&gt;new ClassName(...)&lt;/code&gt; calls that use the pre-deprecation argument count and appends &lt;code&gt;\Drupal::service(\Drupal\image\ImageDerivativeUtilities::class)&lt;/code&gt; as the final argument.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new \Drupal\image\Plugin\Field\FieldFormatter\ImageFormatter(
    $pluginId, $pluginDef, $fieldDef, $settings, $label, $viewMode, $thirdParty,
    $currentUser, $imageStyleStorage, $fileUrlGenerator
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;new \Drupal\image\Plugin\Field\FieldFormatter\ImageFormatter(
    $pluginId, $pluginDef, $fieldDef, $settings, $label, $viewMode, $thirdParty,
    $currentUser, $imageStyleStorage, $fileUrlGenerator,
    \Drupal::service(\Drupal\image\ImageDerivativeUtilities::class)
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Handles direct &lt;code&gt;new ClassName(...)&lt;/code&gt; instantiation only. Contrib subclasses that call &lt;code&gt;parent::__construct()&lt;/code&gt; without the new argument, and any &lt;code&gt;new static(...)&lt;/code&gt; patterns in factory methods, require manual updates: accept &lt;code&gt;?ImageDerivativeUtilities $imageDerivativeUtilities = NULL&lt;/code&gt; as a new constructor parameter, pass it to &lt;code&gt;parent::__construct()&lt;/code&gt;, and inject &lt;code&gt;\Drupal::service(ImageDerivativeUtilities::class)&lt;/code&gt; from the &lt;code&gt;create()&lt;/code&gt; factory.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\New_;
use Rector\Rector\AbstractRector;
use Rector\Config\RectorConfig;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class AddImageDerivativeUtilitiesToImageFormatterRector extends AbstractRector
{
    // Maps each affected class FQCN to the argument count BEFORE the new
    // $imageDerivativeUtilities parameter was added in drupal:11.4.0.
    private const CLASS_OLD_ARG_COUNT = [
        'Drupal\\image\\Plugin\\Field\\FieldFormatter\\ImageFormatter' =&amp;gt; 10,
        'Drupal\\image\\Plugin\\Field\\FieldFormatter\\ImageUrlFormatter' =&amp;gt; 9,
        'Drupal\\media\\Plugin\\Field\\FieldFormatter\\MediaThumbnailFormatter' =&amp;gt; 11,
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Add the missing $imageDerivativeUtilities service argument to ImageFormatter, ImageUrlFormatter, and MediaThumbnailFormatter constructor calls deprecated in drupal:11.4.0.',
            [new CodeSample(
                'new \\Drupal\\image\\Plugin\\Field\\FieldFormatter\\ImageFormatter($pluginId, $pluginDef, $fieldDef, $settings, $label, $viewMode, $thirdParty, $currentUser, $imageStyleStorage, $fileUrlGenerator);',
                'new \\Drupal\\image\\Plugin\\Field\\FieldFormatter\\ImageFormatter($pluginId, $pluginDef, $fieldDef, $settings, $label, $viewMode, $thirdParty, $currentUser, $imageStyleStorage, $fileUrlGenerator, \\Drupal::service(\\Drupal\\image\\ImageDerivativeUtilities::class));',
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [New_::class];
    }

    /** @param New_ $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof New_) {
            return null;
        }
        foreach (self::CLASS_OLD_ARG_COUNT as $fqcn =&amp;gt; $oldArgCount) {
            if (!$this-&amp;gt;isName($node-&amp;gt;class, $fqcn)) {
                continue;
            }
            if (count($node-&amp;gt;args) !== $oldArgCount) {
                return null;
            }
            $serviceCall = $this-&amp;gt;nodeFactory-&amp;gt;createStaticCall(
                'Drupal',
                'service',
                [$this-&amp;gt;nodeFactory-&amp;gt;createClassConstReference('Drupal\\image\\ImageDerivativeUtilities')]
            );
            $node-&amp;gt;args[] = $this-&amp;gt;nodeFactory-&amp;gt;createArg($serviceCall);
            return $node;
        }
        return null;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([AddImageDerivativeUtilitiesToImageFormatterRector::class]);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace deprecated locale submit callbacks with service method calls</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-deprecated-locale-submit-callbacks-with-service-3595084.php</link>
      <guid isPermaLink="false">019efa8f-2d46-701a-b713-1dde64e4cbd6</guid>
      <pubDate>Wed, 24 Jun 2026 16:55:47 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3595084"&gt;#3595084&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.5.0, removed in Drupal 13.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Rewrites direct calls to three deprecated &lt;code&gt;locale.module&lt;/code&gt; procedural functions — &lt;code&gt;locale_system_set_config_langcodes()&lt;/code&gt;, &lt;code&gt;locale_form_language_admin_add_form_alter_submit()&lt;/code&gt;, and &lt;code&gt;locale_form_language_admin_edit_form_alter_submit()&lt;/code&gt; — to their respective service-based replacements. The first delegates to &lt;code&gt;locale.config_manager&lt;/code&gt;, while the latter two delegate to &lt;code&gt;Drupal\locale\Hook\LocaleFormHooks&lt;/code&gt;. All three were deprecated in Drupal 11.5.0.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;locale_system_set_config_langcodes();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;\Drupal::service('locale.config_manager')-&amp;gt;updateDefaultConfigLangcodes();
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Does not rewrite string-literal callback references such as &lt;code&gt;$form['#submit'][] = 'locale_form_language_admin_add_form_alter_submit'&lt;/code&gt;; those must be updated manually to a service-based callable.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\FuncCall;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Expr\StaticCall;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class LocaleDeprecatedCallbacksRector extends AbstractRector
{
    // Maps deprecated function name =&amp;gt; [service_id, replacement_method]
    private const REPLACEMENTS = [
        'locale_system_set_config_langcodes' =&amp;gt; ['locale.config_manager', 'updateDefaultConfigLangcodes'],
        'locale_form_language_admin_add_form_alter_submit' =&amp;gt; ['Drupal\\locale\\Hook\\LocaleFormHooks', 'formLanguageAdminAddFormAlterSubmit'],
        'locale_form_language_admin_edit_form_alter_submit' =&amp;gt; ['Drupal\\locale\\Hook\\LocaleFormHooks', 'formLanguageAdminEditFormAlterSubmit'],
    ];

    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            'Replace deprecated locale procedural submit callbacks with service method calls.',
            [new CodeSample(
                'locale_system_set_config_langcodes();',
                &amp;quot;\\Drupal::service('locale.config_manager')-&amp;gt;updateDefaultConfigLangcodes();&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [FuncCall::class];
    }

    /** @param FuncCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof FuncCall) {
            return null;
        }
        $name = $this-&amp;gt;getName($node-&amp;gt;name);
        if ($name === null || !isset(self::REPLACEMENTS[$name])) {
            return null;
        }
        [$serviceId, $method] = self::REPLACEMENTS[$name];
        $serviceCall = new StaticCall(
            new FullyQualified('Drupal'),
            'service',
            [new Arg(new String_($serviceId))],
        );
        return new MethodCall($serviceCall, $method, $node-&amp;gt;args);
    }
}

return RectorConfig::configure()-&amp;gt;withRules([LocaleDeprecatedCallbacksRector::class]);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    <item>
      <title>Replace CsrfTokenGenerator::get('rest') with TOKEN_KEY constant</title>
      <link>https://github.com/dbuytaert/drupal-digests/blob/main/rector/rules/replace-csrftokengenerator-get-rest-with-token-key-constant-3585891.php</link>
      <guid isPermaLink="false">019ebd9e-0f1f-7340-8781-ff0f4e2850d9</guid>
      <pubDate>Fri, 12 Jun 2026 20:55:12 +0000</pubDate>
      <description>&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Source:&lt;/strong&gt; &lt;a href="https://www.drupal.org/node/3585891"&gt;#3585891&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Timeline:&lt;/strong&gt; Deprecated in Drupal 11.4.0, removed in Drupal 12.0.0&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Replaces &lt;code&gt;CsrfTokenGenerator::get('rest')&lt;/code&gt; with &lt;code&gt;CsrfTokenGenerator::get(CsrfRequestHeaderAccessCheck::TOKEN_KEY)&lt;/code&gt;. The &lt;code&gt;'rest'&lt;/code&gt; key for CSRF token generation was deprecated in &lt;code&gt;drupal:11.4.0&lt;/code&gt; and will be removed in &lt;code&gt;drupal:12.0.0&lt;/code&gt;; old sessions using that key are no longer supported. Contrib modules that implement custom login or REST authentication flows by generating tokens with the old key benefit from this rule.&lt;/p&gt;
&lt;h2&gt;Before&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$this-&amp;gt;csrfToken-&amp;gt;get('rest');
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;After&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;$this-&amp;gt;csrfToken-&amp;gt;get(\Drupal\Core\Access\CsrfRequestHeaderAccessCheck::TOKEN_KEY);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Caveats&lt;/h2&gt;
&lt;p&gt;Only fires when the receiver is typed as &lt;code&gt;CsrfTokenGenerator&lt;/code&gt; (the concrete class, which has no interface). Untyped or dynamically-resolved CSRF service calls (e.g. via &lt;code&gt;\Drupal::service('csrf_token')-&amp;gt;get('rest')&lt;/code&gt;) are not matched because PHPStan cannot infer the receiver type from a generic service-locator call.&lt;/p&gt;
&lt;h2&gt;Code&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-php"&gt;&amp;lt;?php
declare(strict_types=1);

use PhpParser\Node;
use PhpParser\Node\Arg;
use PhpParser\Node\Expr\ClassConstFetch;
use PhpParser\Node\Expr\MethodCall;
use PhpParser\Node\Name\FullyQualified;
use PhpParser\Node\Scalar\String_;
use PHPStan\Type\ObjectType;
use Rector\Config\RectorConfig;
use Rector\Rector\AbstractRector;
use Symplify\RuleDocGenerator\ValueObject\CodeSample\CodeSample;
use Symplify\RuleDocGenerator\ValueObject\RuleDefinition;

final class CsrfTokenGetRestKeyRector extends AbstractRector
{
    public function getRuleDefinition(): RuleDefinition
    {
        return new RuleDefinition(
            &amp;quot;Replace CsrfTokenGenerator::get('rest') with get(CsrfRequestHeaderAccessCheck::TOKEN_KEY), deprecated in drupal:11.4.0.&amp;quot;,
            [new CodeSample(
                &amp;quot;\$this-&amp;gt;csrfToken-&amp;gt;get('rest');&amp;quot;,
                &amp;quot;\$this-&amp;gt;csrfToken-&amp;gt;get(\Drupal\Core\Access\CsrfRequestHeaderAccessCheck::TOKEN_KEY);&amp;quot;,
            )],
        );
    }

    /** @return array&amp;lt;class-string&amp;lt;Node&amp;gt;&amp;gt; */
    public function getNodeTypes(): array
    {
        return [MethodCall::class];
    }

    /** @param MethodCall $node */
    public function refactor(Node $node): ?Node
    {
        if (!$node instanceof MethodCall) {
            return null;
        }
        if (!$this-&amp;gt;isName($node-&amp;gt;name, 'get')) {
            return null;
        }
        if (!$this-&amp;gt;isObjectType($node-&amp;gt;var, new ObjectType('Drupal\Core\Access\CsrfTokenGenerator'))) {
            return null;
        }
        if (count($node-&amp;gt;args) !== 1) {
            return null;
        }
        $firstArg = $node-&amp;gt;args[0];
        if (!$firstArg instanceof Arg) {
            return null;
        }
        if (!$firstArg-&amp;gt;value instanceof String_) {
            return null;
        }
        if ($firstArg-&amp;gt;value-&amp;gt;value !== 'rest') {
            return null;
        }
        $node-&amp;gt;args[0] = new Arg(
            new ClassConstFetch(
                new FullyQualified('Drupal\Core\Access\CsrfRequestHeaderAccessCheck'),
                'TOKEN_KEY'
            )
        );
        return $node;
    }
}

return RectorConfig::configure()-&amp;gt;withRules([CsrfTokenGetRestKeyRector::class]);
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;p&gt;&lt;em&gt;This content is AI-generated and may contain errors. See &lt;a href="https://github.com/dbuytaert/drupal-digests/"&gt;Drupal Digests&lt;/a&gt; for more.&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
  </channel>
</rss>