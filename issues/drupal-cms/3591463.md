# #3591463: Fix contrast in "Add a page" button on /admin/dashboard

- **Project:** Drupal CMS
- **Type:** Normal unknown (0)
- **Status:** Fixed
- **Discussion:** [#3591463](https://git.drupalcode.org/project/drupal_cms/-/work_items/3591463) · 1 contributor · 3 comments

## Summary

On a fresh install of Drupal CMS using the "Starter" site template, the blue "Add a page" button shown on the admin dashboard (`/admin/dashboard`) was illegible because its text did not have enough contrast against its background. This is exactly the kind of first-impression problem a new site owner would hit immediately after installation, since it's one of the first calls to action they see. The issue was confirmed in both Chrome and Firefox and has been fixed, so the button is now readable out of the box.

## Impact

No upgrade or configuration changes required.

## Technical details

The reported defect was specifically the low-contrast styling of the dashboard's primary "Add a page" action button, present right after installing the Starter site template and visible on the `/admin/dashboard` route. No merge request or commit referencing this issue could be located in the current `drupal_cms` repository history, so the specific CSS or markup change that resolved it cannot be verified here; only the reported symptom and the issue's "fixed" resolution are confirmed.

## Contribution

### Timeline

- Opened: September 17, 2026

---

*This content is AI-generated and may contain errors. See [Drupal Digests](https://github.com/dbuytaert/drupal-digests/) for more.*
