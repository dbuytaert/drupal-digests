# #3301682: Define bundle classes via attributes

- **Project:** Drupal Core
- **Type:** Normal feature request
- **Version:** main
- **Status:** Fixed
- **Changes:** 19 files, +472/−4 · php +465/−4 · yml +7
- **Diff:** [ff83524](https://git.drupalcode.org/project/drupal/-/commit/ff835241c95ec7d815b882e8c3087d79b0415ecb)
- **Discussion:** [#3301682](https://www.drupal.org/node/3301682) · 14 contributors · 77 comments
- **Followers:** 33

## Summary

Bundle classes can now be registered by placing a `#[Bundle]` attribute on a class in a module's `Entity/` namespace, instead of implementing `hook_entity_bundle_info_alter()`. This removes boilerplate when a project has many bundle classes spread across many modules. The attribute accepts `entityType`, `bundle`, `label`, and `translatable` parameters. The `hook_entity_bundle_info_alter()` approach still works and can override attribute-provided values.

## Impact

Affects module maintainers.

- **New API:** New `\Drupal\Core\Entity\Attribute\Bundle` attribute for declaring bundle classes without `hook_entity_bundle_info_alter()`

## Technical details

Declaring a bundle class previously required implementing `hook_entity_bundle_info_alter()` and setting the class for each bundle. With many bundle classes across many modules, this meant many hook implementations and `use` statements in `.module` files.

The new `\Drupal\Core\Entity\Attribute\Bundle` attribute class (`core/lib/Drupal/Core/Entity/Attribute/Bundle.php`) extends `ContentEntityType`, which itself extends `EntityType`. Because `EntityTypeManager` already discovers classes with `EntityType` attributes under each module's `Entity/` subdirectory (recursively), bundle classes are picked up by the existing attribute discovery without introducing a new discovery mechanism. The issue explored three other approaches (a plugin manager, a compiler pass, and a standalone discovery class in `EntityTypeManager::findDefinitions()`) before settling on this derivative-based design proposed by ghost of drupal past.

The `Bundle` constructor formats its ID as `{entity_type_id}:{bundle}` using `PluginBase::DERIVATIVE_SEPARATOR`, passing that composite string as the `id` to the parent `ContentEntityType` constructor. Bundle metadata (`class`, `label`, `translatable`) is stored in the `entity_type_bundle_info` additional property on the definition object.

In `EntityTypeManager::findDefinitions()`, after discovery runs, definitions whose IDs contain `:` are filtered out of the normal entity type map. Their bundle data is collected and then attached to the corresponding parent entity type definition via the `entity_type_bundle_info` property, after `entity_type_build` hooks have run and just before `entity_type_alter` hooks run. `EntityTypeBundleInfo::getAllBundleInfo()` reads that property and merges the bundle class, label, and translatable info into the bundle array for the entity type. The processing order is: `hook_entity_bundle_info` runs first, then attribute bundle info is applied, then `hook_entity_bundle_info_alter` runs. Optional attribute properties (`label`, `translatable`) that are `NULL` are excluded via `array_filter` and do not override values already set by hooks.

For entity types that define a `bundle_entity_type` (for example, nodes use `node_type`), the attribute cannot create new bundles. If the bundle entity does not already exist in `$this->bundleInfo`, the attribute info is silently skipped (a `continue` statement, not an exception) per berdir's suggestion. This prevents invalid states where bundles exist without their expected config entities. For entity types without a `bundle_entity_type`, the attribute can define entirely new bundles.

`EntityType::__construct()` was updated to handle derivative IDs: if the ID contains `:`, the entity type part and the bundle part are validated separately against `ID_MAX_LENGTH` and `BUNDLE_MAX_LENGTH` (both 32 characters). `EntityTypeManager::getDefinition()` throws a `\LogicException` if called with a derivative ID, preventing bundle definitions from being accessed as entity types.

If multiple modules provide a `#[Bundle]` attribute for the same entity type and bundle, the last one discovered wins (discovery order follows module order in `container.namespaces`). `hook_entity_bundle_info_alter` can be used to explicitly choose which class should take effect.

## Upgrade

To use the new attribute, place `#[Bundle]` on a class extending the entity type's base class in your module's `Entity/` namespace:

```php
use Drupal\Core\Entity\Attribute\Bundle;
use Drupal\Core\StringTranslation\TranslatableMarkup;
use Drupal\node\Entity\Node;

#[Bundle(
  entityType: 'node',
  bundle: 'article',
  label: new TranslatableMarkup('Article'),
)]
class Article extends Node {}
```

The corresponding `hook_entity_bundle_info_alter()` implementation can then be removed. The `hook_entity_bundle_info_alter()` still works and takes precedence over attributes, so both approaches can coexist.

Projects migrating from the contrib [Bundle Class Attribute](https://www.drupal.org/project/bca) module need to update the attribute namespace from the contrib `Bundle` to `\Drupal\Core\Entity\Attribute\Bundle`. The `entityType` named parameter is the same in both, so no parameter renaming is required.

## Contribution

### Timeline

- Opened: August 3, 2022
- Committed: April 10, 2026 (3 years and 2 commits later)

### Key contributors

- dpi (PreviousNext)
- mstrelan (PreviousNext)
- avpaderno
- berdir (MD Systems GmbH)
- godotislate
- ghost of drupal past
- catch (Third and Grove)
- acbramley (PreviousNext)

### Collaboration

mstrelan opened the issue and created two merge requests exploring different discovery approaches: MR 5178 (plugin manager) and MR 11545 (compiler pass). godotislate then proposed a third approach in MR 14763, adding bundle class discovery directly inside `EntityTypeManager::findDefinitions()`, but closed it the same day after ghost of drupal past proposed a fourth, derivative-based approach that reused existing entity plugin discovery without adding new infrastructure. godotislate implemented that fourth approach in MR 14764. berdir identified the problem of attributes silently creating bundles for entity types that expect all bundles to be backed by config entities (those with a `bundle_entity_type`), leading to the safeguard that skips nonexistent bundle entities rather than throwing an exception, so that deleting a bundle entity does not immediately break the site.

---

*This content is AI-generated and may contain errors. See [Drupal Digests](https://github.com/dbuytaert/drupal-digests/) for more.*
